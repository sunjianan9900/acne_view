---
description: 每次完成任务后自动 git add 并 commit，使用中文简短说明
alwaysApply: true
---

# 任务完成后自动提交

完成任何代码或配置改动后，**必须**在回复用户前执行 git 提交，不得跳过。

## 何时提交

- 完成用户指派的一个任务或子任务
- 对项目文件做了任何有意义的修改（代码、配置、文档、规则等）
- 仅做只读分析、回答问题、未改文件时，不提交

## 提交流程

1. `git status` 确认本次改动涉及的文件
2. `git add` 仅添加与本次任务相关的文件（不要 `git add .` 盲目全加）
3. 用 HEREDOC 格式提交，commit message 使用**简体中文**，1–2 句，说清「做了什么」而非罗列文件名

```bash
git commit -m "$(cat <<'EOF'
修复登录页表单校验逻辑

EOF
)"
```

4. 提交后 `git status` 确认成功

## Commit 写法

- 用动词开头：新增、修复、调整、重构、删除、更新
- 简短具体，例如：`新增用户头像上传接口`、`修复首页列表加载失败`
- 不要写 `update`、`fix bug`、`WIP` 等含糊英文

## 注意

- 不提交含密钥的文件（`.env`、凭证等）；若被误改，警告用户并排除
- 不执行 `git push`，除非用户明确要求
- 不使用 `--no-verify` 等跳过 hook 的参数
- pre-commit hook 失败时：修复问题后**新建** commit，不要 amend 失败的提交
